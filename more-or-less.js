const more = (n) => n+1
const resMore = more(n, 1)
const less = (n) => n-1
const resLess = less(n, 1)
const add = (a, b) => a+b
const resAdd = add(a, b)
const sub = (x, y) => x-y
const resSub = sub(x, y)